let buttonOnPage = document.getElementById('btn');
let bodyElement = document.getElementsByTagName('body');

buttonOnPage.addEventListener('click', clickme);
let arr =['green', 'red', 'yellow', 'pink', 'blue', 'brown']

function generateColor() {
    let color = '#';
}
for (let i =0; i++; i < arr.length){
console.log(color);
}

arr.map((item) => {
    const button = document.createElement('button');
    button.style.backgroundColor = (changebgColor);
    button.addEventListener ('click', () => {
    });
}
);
colorOptions.appendChild(button);
function clickme() {
   
    let arr = generateColor();
    document.body.style.backgroundColor = 'blue', 'yellow';
    
}